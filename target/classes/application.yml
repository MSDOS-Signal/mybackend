spring:
  application:
    name: XinhaoChat
  datasource:
    # 改用环境变量读取，避免硬编码
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://hopper.proxy.rlwy.net:21191/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&databaseName=xinhao_chat}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:zWzRSLEMaKVrYBgdxDAtjGymJjPHCrLp}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
# Render会自动分配PORT环境变量，必须读取这个端口
server:
  port: ${PORT:8080}